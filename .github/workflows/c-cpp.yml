name: C/C++ CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container: epitechcontent/epitest-docker:latest
    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
    - name: make fclean
      run: make fclean
    - name: make tests_run
      run: make tests_run
